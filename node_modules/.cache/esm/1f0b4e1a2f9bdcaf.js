let tmi,botUsername,oAuthToken,channelName;_2f9‍.w("tmi.js",[["default",["tmi"],function(v){tmi=v}]]);_2f9‍.w("./auth",[["botUsername",["botUsername"],function(v){botUsername=v}],["oAuthToken",["oAuthToken"],function(v){oAuthToken=v}],["channelName",["channelName"],function(v){channelName=v}]]);


const client = new tmi.Client({
	options: { debug: true, messagesLogLevel: "info" },
	connection: {
		reconnect: true,
		secure: true
	},
	identity: {
		username: botUsername,
		password: `oauth:${oAuthToken}`
	},
	channels: [ channelName ]
});

client.connect().catch(_2f9‍.g.console.error);

client.on('message', (channel, userstate, message, self) => {
	if(self) return;
	if(message.toLowerCase() === '!hello') {
		client.say(channel, `@${userstate.username}, heya!`);
	}
});